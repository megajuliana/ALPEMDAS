#include <iostream>
using namespace std;

int main() {
    char warnaLampu;
    cout << "--- Simulasi Lalu Lintas ---" << endl;
    cout << "Masukkan huruf awal warna lampu (M/m = Merah, K/k = Kuning, H/h = Hijau): ";

    cin >> warnaLampu;
    if (warnaLampu == 'M' || warnaLampu == 'm') { 
    } 
    else if (warnaLampu == 'K' || warnaLampu == 'k') {
    } 
    else if (warnaLampu == 'H' || warnaLampu == 'h') {
    } 
    else {
    }
    cout << "Program Selesai." << endl; 
}
